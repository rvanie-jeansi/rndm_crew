import { ScreenStub } from '@/components/screen-stub';

export default function ProfileScreen() {
  return (
    <ScreenStub
      emoji="👤"
      title="Профиль"
      subtitle="Твой уровень, накопленный опыт и достижения появятся здесь"
    />
  );
}