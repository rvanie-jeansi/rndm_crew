import { ScreenStub } from '@/components/screen-stub';

export default function MapScreen() {
  return (
    <ScreenStub
      emoji="🗺️"
      title="Карта"
      subtitle="Здесь появятся твоя позиция, маршрут приключения и посещённые места"
    />
  );
}